#include <iostream>
#include <ctime>

using namespace std;

int main() {

    srand(static_cast<unsigned int>(time(0)));

    int HP;
    int strength;

    //not sure how much of each so I did 0 - 50
    HP = rand() % 51;
    strength = rand() % 51;

    return 0;
}
